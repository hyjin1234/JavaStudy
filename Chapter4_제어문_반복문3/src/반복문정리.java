/*
 *   자바 /오라클 /jsp/Spring/javaScript(Ajax, VueJS , ReactJS)
 *   ----------------------------------AWS
 *   
 *   ***page 156~157(for)
 *        => 반복횟수를 알고 있는 상태(화면 UI)
 *        => 형식
 *          =초기값 ==>
 *          =조건식
 *            범위 지정 ==>횟수
 *          =증감식(++,--) =>쿠키에 저장 
 *           여러개 ==> 복합 대입연산자 사용 += ,-=
 *           for(초기값;조건문;증가식)
 *           {
 *             출력내용
 *           }
 *           초기값=> 조건문=> 출력내용=>증가식=>조건문(반복, 조건=>false이면 종료)
 *           
 *   page 167~168(while)
 *        =>데이터베이스 , 횟수를 모르는 상태
 *        => 조건문을 생략할 수 없다
 *        초기값
 *        while(조건식){ ==>무한루프
 *           반복수행문장
 *           증감식
 *           }
 *           
 *           --------------
 *           while(true) ==> 처음으로 돌아간다
 *           {
 *             반복수행문장
 *             if(){
 *                종료조건
 *             }
 *            }
 *   page 175(break)
 *        => switch~case ,반복문에서만 사용이 가능(단독 if문사용시 오류), break가있는 반복문만 제어가능
 *           반복문을 종료
 *   page 176(continue)
 *        => 반복문에서만 사용가능 , continue만 있는 반복문만 제어가 가능
 *        => 특정 부분을 제외 할대, 처음으로 시작할때
 *        => for => 증가식으로 이동, while,do while=> 조건식으로 이동
 */
public class 반복문정리 {

	public static void main(String[] args) {
		// TODO Auto-generated method stub

	}

}
